needs `touch nginx.conf` before starting so docker-compose don't confuse file with folder.

also look out for `GENERATED ROOT 
PASSWORD: ...` in logs from first start 
and note your mysql root password.
